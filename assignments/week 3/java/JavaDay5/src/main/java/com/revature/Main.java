package com.revature;

public class Main {
    public static void main(String[] args) {
        System.out.println("Hello world!");
    }


    //Palindrome answer stored in here :)

    /*
        //Your code here

        str = str.toLowerCase().replaceAll( " ", "");
        String rev = "";
        boolean s = false;

        //Reverse string to check against input
        for (int i = str.length() - 1; i >= 0; i--) {
            rev = rev + str.charAt(i);
        }

        //Check if str = rev
        if (str.equals(rev)) {
            s = true;
        }

        return s;
     */
}